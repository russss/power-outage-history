name: Scrape latest data

on:
  push:
  workflow_dispatch:
  schedule:
    - cron:  '2,22,42 * * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Check out this repo
      uses: actions/checkout@v2
    - name: Fetch latest data
      run: |-
        curl https://ssen-powertrack-api.opcld.com/gridiview/reporter/info | jq . > ssen.json
        curl 'https://www.enwl.co.uk/power-outages/search?pageSize=1000&postcodeOrReferenceNumber=&pageNumber=1&includeCurrent=true&includeResolved=false&includeTodaysPlanned=false&includeFuturePlanned=false&includeCancelledPlanned=false' | jq . > enwl.json
    - name: Commit and push if it changed
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest data: ${timestamp}" || exit 0
        git push
